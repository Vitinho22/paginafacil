/**
 * PáginaFácil usa CSS puro em app/globals.css.
 * Esta configuração vazia impede que configurações antigas do Tailwind
 * presentes na pasta sejam carregadas pelo Next.js/Turbopack.
 */
const config = {
  plugins: {},
};

export default config;
